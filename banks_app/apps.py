"""Django application configuration for the banks_app."""

from django.apps import AppConfig


class BanksAppConfig(AppConfig):
    """Configuration for the banks_app Django application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'banks_app'
